---
alwaysApply: true
---

# ❗ Tratamento de Exceções

As exceções lançadas na aplicação devem seguir um padrão centralizado com `CustomHttpException` e `ErrorMessagesService`.

## ✅ Regras

- Usar `CustomHttpException` em toda exceção controlada.
- Obter mensagens padronizadas do `ErrorMessagesService`.
- Registrar logs com `Logger` se necessário.

## 📌 Exemplo

```ts
if (!establishment) {
  const message = this.errorMessagesService.getMessage(
    ErrorCode.ESTABLISHMENT_NOT_FOUND_OR_ACCESS_DENIED,
    { ESTABLISHMENT_ID: request.establishmentId, OWNER_ID: ownerId },
  );

  this.logger.warn(message);

  throw new CustomHttpException(
    message,
    HttpStatus.NOT_FOUND,
    ErrorCode.ESTABLISHMENT_NOT_FOUND_OR_ACCESS_DENIED,
  );
}
```